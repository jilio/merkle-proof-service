// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: galactica/merkle/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "galactica.merkle";

/** QueryProofRequest is the request type for the Query.Proof method. */
export interface QueryProofRequest {
  /** registry  is the ZkCertificateRegistry hex address, which starts with 0x. */
  registry: string;
  /** leaf is the leaf uint256 value. */
  leaf: string;
}

/** QueryProofResponse is the response type for the Query.Proof method. */
export interface QueryProofResponse {
  /** proof is the merkle proof. */
  proof: Proof | undefined;
}

/** GetEmptyLeafProofRequest is the request type for the Query.GetEmptyLeafProof method. */
export interface GetEmptyLeafProofRequest {
  /** registry is the ZkCertificateRegistry hex address, which starts with 0x. */
  registry: string;
}

/** GetEmptyIndexResponse is the response type for the Query.GetEmptyLeafProof method. */
export interface GetEmptyLeafProofResponse {
  /** proof is the merkle proof of the empty leaf. */
  proof: Proof | undefined;
}

/** Proof is the merkle proof. */
export interface Proof {
  /** leaf is the leaf value encoded as a string containing the uint256 value. */
  leaf: string;
  /** path is the merkle proof path, encoded as a string containing the uint256 values. */
  path: string[];
  /** index is the leaf index. */
  index: number;
  /** root is the merkle root, value encoded as a string containing the uint256 value. */
  root: string;
}

/** QueryBatchProofsRequest is the request type for the Query.GetBatchProofs method. */
export interface QueryBatchProofsRequest {
  /** registry is the ZkCertificateRegistry hex address, which starts with 0x. */
  registry: string;
  /** operations is the list of batch operations to process. */
  operations: BatchOperation[];
}

/** BatchOperation represents a single operation in the batch. */
export interface BatchOperation {
  /** leaf_hash is the leaf hash (uint256 string). */
  leafHash: string;
  /** is_addition indicates if this is an addition (true) or revocation (false). */
  isAddition: boolean;
}

/** QueryBatchProofsResponse is the response type for the Query.GetBatchProofs method. */
export interface QueryBatchProofsResponse {
  /** initial_root is the merkle root before the batch operations. */
  initialRoot: string;
  /** proofs is the list of batch proofs. */
  proofs: BatchProof[];
}

/** BatchProof represents a proof in the batch. */
export interface BatchProof {
  /** index is the leaf index. */
  index: number;
  /** path is the proof siblings (uint256 strings). */
  path: string[];
}

function createBaseQueryProofRequest(): QueryProofRequest {
  return { registry: "", leaf: "" };
}

export const QueryProofRequest: MessageFns<QueryProofRequest> = {
  encode(message: QueryProofRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registry !== "") {
      writer.uint32(10).string(message.registry);
    }
    if (message.leaf !== "") {
      writer.uint32(18).string(message.leaf);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryProofRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryProofRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registry = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.leaf = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryProofRequest {
    return {
      registry: isSet(object.registry) ? globalThis.String(object.registry) : "",
      leaf: isSet(object.leaf) ? globalThis.String(object.leaf) : "",
    };
  },

  toJSON(message: QueryProofRequest): unknown {
    const obj: any = {};
    if (message.registry !== "") {
      obj.registry = message.registry;
    }
    if (message.leaf !== "") {
      obj.leaf = message.leaf;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryProofRequest>, I>>(base?: I): QueryProofRequest {
    return QueryProofRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryProofRequest>, I>>(object: I): QueryProofRequest {
    const message = createBaseQueryProofRequest();
    message.registry = object.registry ?? "";
    message.leaf = object.leaf ?? "";
    return message;
  },
};

function createBaseQueryProofResponse(): QueryProofResponse {
  return { proof: undefined };
}

export const QueryProofResponse: MessageFns<QueryProofResponse> = {
  encode(message: QueryProofResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proof !== undefined) {
      Proof.encode(message.proof, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryProofResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryProofResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.proof = Proof.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryProofResponse {
    return { proof: isSet(object.proof) ? Proof.fromJSON(object.proof) : undefined };
  },

  toJSON(message: QueryProofResponse): unknown {
    const obj: any = {};
    if (message.proof !== undefined) {
      obj.proof = Proof.toJSON(message.proof);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryProofResponse>, I>>(base?: I): QueryProofResponse {
    return QueryProofResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryProofResponse>, I>>(object: I): QueryProofResponse {
    const message = createBaseQueryProofResponse();
    message.proof = (object.proof !== undefined && object.proof !== null) ? Proof.fromPartial(object.proof) : undefined;
    return message;
  },
};

function createBaseGetEmptyLeafProofRequest(): GetEmptyLeafProofRequest {
  return { registry: "" };
}

export const GetEmptyLeafProofRequest: MessageFns<GetEmptyLeafProofRequest> = {
  encode(message: GetEmptyLeafProofRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registry !== "") {
      writer.uint32(10).string(message.registry);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEmptyLeafProofRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEmptyLeafProofRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registry = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEmptyLeafProofRequest {
    return { registry: isSet(object.registry) ? globalThis.String(object.registry) : "" };
  },

  toJSON(message: GetEmptyLeafProofRequest): unknown {
    const obj: any = {};
    if (message.registry !== "") {
      obj.registry = message.registry;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEmptyLeafProofRequest>, I>>(base?: I): GetEmptyLeafProofRequest {
    return GetEmptyLeafProofRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEmptyLeafProofRequest>, I>>(object: I): GetEmptyLeafProofRequest {
    const message = createBaseGetEmptyLeafProofRequest();
    message.registry = object.registry ?? "";
    return message;
  },
};

function createBaseGetEmptyLeafProofResponse(): GetEmptyLeafProofResponse {
  return { proof: undefined };
}

export const GetEmptyLeafProofResponse: MessageFns<GetEmptyLeafProofResponse> = {
  encode(message: GetEmptyLeafProofResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proof !== undefined) {
      Proof.encode(message.proof, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEmptyLeafProofResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEmptyLeafProofResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.proof = Proof.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEmptyLeafProofResponse {
    return { proof: isSet(object.proof) ? Proof.fromJSON(object.proof) : undefined };
  },

  toJSON(message: GetEmptyLeafProofResponse): unknown {
    const obj: any = {};
    if (message.proof !== undefined) {
      obj.proof = Proof.toJSON(message.proof);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEmptyLeafProofResponse>, I>>(base?: I): GetEmptyLeafProofResponse {
    return GetEmptyLeafProofResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEmptyLeafProofResponse>, I>>(object: I): GetEmptyLeafProofResponse {
    const message = createBaseGetEmptyLeafProofResponse();
    message.proof = (object.proof !== undefined && object.proof !== null) ? Proof.fromPartial(object.proof) : undefined;
    return message;
  },
};

function createBaseProof(): Proof {
  return { leaf: "", path: [], index: 0, root: "" };
}

export const Proof: MessageFns<Proof> = {
  encode(message: Proof, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leaf !== "") {
      writer.uint32(10).string(message.leaf);
    }
    for (const v of message.path) {
      writer.uint32(18).string(v!);
    }
    if (message.index !== 0) {
      writer.uint32(24).uint32(message.index);
    }
    if (message.root !== "") {
      writer.uint32(34).string(message.root);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Proof {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.leaf = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.root = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Proof {
    return {
      leaf: isSet(object.leaf) ? globalThis.String(object.leaf) : "",
      path: globalThis.Array.isArray(object?.path) ? object.path.map((e: any) => globalThis.String(e)) : [],
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      root: isSet(object.root) ? globalThis.String(object.root) : "",
    };
  },

  toJSON(message: Proof): unknown {
    const obj: any = {};
    if (message.leaf !== "") {
      obj.leaf = message.leaf;
    }
    if (message.path?.length) {
      obj.path = message.path;
    }
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.root !== "") {
      obj.root = message.root;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Proof>, I>>(base?: I): Proof {
    return Proof.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Proof>, I>>(object: I): Proof {
    const message = createBaseProof();
    message.leaf = object.leaf ?? "";
    message.path = object.path?.map((e) => e) || [];
    message.index = object.index ?? 0;
    message.root = object.root ?? "";
    return message;
  },
};

function createBaseQueryBatchProofsRequest(): QueryBatchProofsRequest {
  return { registry: "", operations: [] };
}

export const QueryBatchProofsRequest: MessageFns<QueryBatchProofsRequest> = {
  encode(message: QueryBatchProofsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registry !== "") {
      writer.uint32(10).string(message.registry);
    }
    for (const v of message.operations) {
      BatchOperation.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryBatchProofsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryBatchProofsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registry = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.operations.push(BatchOperation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryBatchProofsRequest {
    return {
      registry: isSet(object.registry) ? globalThis.String(object.registry) : "",
      operations: globalThis.Array.isArray(object?.operations)
        ? object.operations.map((e: any) => BatchOperation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryBatchProofsRequest): unknown {
    const obj: any = {};
    if (message.registry !== "") {
      obj.registry = message.registry;
    }
    if (message.operations?.length) {
      obj.operations = message.operations.map((e) => BatchOperation.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryBatchProofsRequest>, I>>(base?: I): QueryBatchProofsRequest {
    return QueryBatchProofsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryBatchProofsRequest>, I>>(object: I): QueryBatchProofsRequest {
    const message = createBaseQueryBatchProofsRequest();
    message.registry = object.registry ?? "";
    message.operations = object.operations?.map((e) => BatchOperation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBatchOperation(): BatchOperation {
  return { leafHash: "", isAddition: false };
}

export const BatchOperation: MessageFns<BatchOperation> = {
  encode(message: BatchOperation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leafHash !== "") {
      writer.uint32(10).string(message.leafHash);
    }
    if (message.isAddition !== false) {
      writer.uint32(16).bool(message.isAddition);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchOperation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchOperation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.leafHash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isAddition = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchOperation {
    return {
      leafHash: isSet(object.leafHash) ? globalThis.String(object.leafHash) : "",
      isAddition: isSet(object.isAddition) ? globalThis.Boolean(object.isAddition) : false,
    };
  },

  toJSON(message: BatchOperation): unknown {
    const obj: any = {};
    if (message.leafHash !== "") {
      obj.leafHash = message.leafHash;
    }
    if (message.isAddition !== false) {
      obj.isAddition = message.isAddition;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchOperation>, I>>(base?: I): BatchOperation {
    return BatchOperation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchOperation>, I>>(object: I): BatchOperation {
    const message = createBaseBatchOperation();
    message.leafHash = object.leafHash ?? "";
    message.isAddition = object.isAddition ?? false;
    return message;
  },
};

function createBaseQueryBatchProofsResponse(): QueryBatchProofsResponse {
  return { initialRoot: "", proofs: [] };
}

export const QueryBatchProofsResponse: MessageFns<QueryBatchProofsResponse> = {
  encode(message: QueryBatchProofsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.initialRoot !== "") {
      writer.uint32(10).string(message.initialRoot);
    }
    for (const v of message.proofs) {
      BatchProof.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryBatchProofsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryBatchProofsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.initialRoot = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.proofs.push(BatchProof.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryBatchProofsResponse {
    return {
      initialRoot: isSet(object.initialRoot) ? globalThis.String(object.initialRoot) : "",
      proofs: globalThis.Array.isArray(object?.proofs) ? object.proofs.map((e: any) => BatchProof.fromJSON(e)) : [],
    };
  },

  toJSON(message: QueryBatchProofsResponse): unknown {
    const obj: any = {};
    if (message.initialRoot !== "") {
      obj.initialRoot = message.initialRoot;
    }
    if (message.proofs?.length) {
      obj.proofs = message.proofs.map((e) => BatchProof.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryBatchProofsResponse>, I>>(base?: I): QueryBatchProofsResponse {
    return QueryBatchProofsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryBatchProofsResponse>, I>>(object: I): QueryBatchProofsResponse {
    const message = createBaseQueryBatchProofsResponse();
    message.initialRoot = object.initialRoot ?? "";
    message.proofs = object.proofs?.map((e) => BatchProof.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBatchProof(): BatchProof {
  return { index: 0, path: [] };
}

export const BatchProof: MessageFns<BatchProof> = {
  encode(message: BatchProof, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    for (const v of message.path) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchProof {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchProof {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      path: globalThis.Array.isArray(object?.path) ? object.path.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: BatchProof): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.path?.length) {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchProof>, I>>(base?: I): BatchProof {
    return BatchProof.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchProof>, I>>(object: I): BatchProof {
    const message = createBaseBatchProof();
    message.index = object.index ?? 0;
    message.path = object.path?.map((e) => e) || [];
    return message;
  },
};

/** Query defines the gRPC querier service. */
export interface Query {
  /** Proof queries the proof of a leaf in the merkle tree. */
  Proof(request: QueryProofRequest): Promise<QueryProofResponse>;
  /** GetEmptyLeafProof queries the proof of the any empty leaf in the merkle tree. */
  GetEmptyLeafProof(request: GetEmptyLeafProofRequest): Promise<GetEmptyLeafProofResponse>;
  /** GetBatchProofs queries proofs for a batch of additions or revocations. */
  GetBatchProofs(request: QueryBatchProofsRequest): Promise<QueryBatchProofsResponse>;
}

export const QueryServiceName = "galactica.merkle.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Proof = this.Proof.bind(this);
    this.GetEmptyLeafProof = this.GetEmptyLeafProof.bind(this);
    this.GetBatchProofs = this.GetBatchProofs.bind(this);
  }
  Proof(request: QueryProofRequest): Promise<QueryProofResponse> {
    const data = QueryProofRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Proof", data);
    return promise.then((data) => QueryProofResponse.decode(new BinaryReader(data)));
  }

  GetEmptyLeafProof(request: GetEmptyLeafProofRequest): Promise<GetEmptyLeafProofResponse> {
    const data = GetEmptyLeafProofRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetEmptyLeafProof", data);
    return promise.then((data) => GetEmptyLeafProofResponse.decode(new BinaryReader(data)));
  }

  GetBatchProofs(request: QueryBatchProofsRequest): Promise<QueryBatchProofsResponse> {
    const data = QueryBatchProofsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetBatchProofs", data);
    return promise.then((data) => QueryBatchProofsResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
